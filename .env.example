# Copy to .env (or let `make setup` create it) to keep the Hugging Face cache
# inside the checkout. uv loads this file for `uv run` via UV_ENV_FILE.
#
# Absolute paths are required here; uv does not expand variables in .env.
HF_HOME=/absolute/path/to/YuE/.hf
